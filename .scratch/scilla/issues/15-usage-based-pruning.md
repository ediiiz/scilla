# Usage-based pruning

Type: research
Status: open
Blocked by:

## Question

`scilla usage` reads local agent transcripts (Claude Code keeps them under `~/.claude/projects`) to report which installed skills actually get triggered, and suggests removing unused ones. Research first: the transcript format and how a skill activation shows up in it, other agents' transcript locations, and privacy. It must stay local-only; nothing is sent anywhere.
